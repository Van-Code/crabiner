# Web Client Environment Variables

# API Base URL
# For development with local backend
API_BASE_URL=http://localhost:3000

# For production
# API_BASE_URL=https://api.crabiner.com

# Note: The web client is served as static files from the backend's /public directory
# This .env.example file is for future web client build tooling if needed
# Currently, the API base URL is implicit (same-origin)
